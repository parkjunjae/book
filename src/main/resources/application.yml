spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
      ddl-auto: none # 'create', 'update', 'validate', 'none' 중 선택
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8 이상에 맞는 Dialect
        format_sql: true
  security:
    user:
      password: '12345'
      name: 'eazybytes'

logging:
  level:
    org.hibernate.SQL: debug


